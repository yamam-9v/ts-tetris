# TypeScript学習プロジェクト指示書 — Tetris

このドキュメントは Claude Code に渡す前提で書かれている。
プロジェクトルートに置き、必要なら `CLAUDE.md` として読み込ませる。

---

## 1. このプロジェクトの目的

**成果物を作ることではなく、TypeScriptを身につけることが目的。**
動くテトリスはその副産物として得られればよい。

したがって以下を優先する。

- 実装スピードより理解の深さ
- 「動いたからOK」ではなく「なぜこの型なのか説明できる」状態
- コードの正しさより、書いた本人が後から読めること

### 学習者の前提

| 項目 | 状態 |
|---|---|
| HTML / CSS | 触ったことがある |
| JavaScript | ほんの少し触った程度 |
| TypeScript | 未経験(このプロジェクトが初) |
| Python | 使用経験あり(型ヒントの概念はある程度わかる) |
| Docker | ローカル開発で使用中 |
| ターミナル | WezTerm を常用 |
| Git / GitHub Actions | 使用経験あり |

---

## 2. Claude Code への依頼事項

### 2.1 出力スタイル

**Learning スタイルを使用する。**

- `/config` → Output style → Learning を選択
- 出力スタイルはセッション開始時にシステムプロンプトへ入るため、変更後は `/clear` または新規セッションが必要

### 2.2 コードの書き方に関するお願い

- **`TODO(human)` を積極的に置くこと。** 特に以下は原則として学習者に書かせる。
  - 純粋関数の中身(衝突判定、回転、ライン消去などのロジック本体)
  - 型定義そのもの(まず学習者に書かせ、その後レビューする)
- **一方、以下は Claude が書いてよい。** ここで消耗させたくない。
  - 設定ファイル(`tsconfig.json`, `vite.config.ts`, `devcontainer.json`)
  - Canvas / DOM の定型的な描画セットアップ
  - テストのボイラープレート

- **一度に大量のコードを生成しない。** 1回のやり取りで扱うのは、原則1ファイル・1関数まで。
- 学習者が `TODO(human)` を埋めたら、**動くかどうかだけでなく、型の付け方についてもフィードバックする。**
- より良い書き方がある場合は、いきなり書き換えず「なぜそちらが良いか」を先に説明する。

### 2.3 詰まったときの切り分け

学習者は JS と TS の区別がまだ付いていない。エラーが出たとき、**それが JS 由来か TS 由来かを毎回明示すること。**

| 症状 | 分類 |
|---|---|
| `undefined` が出る / `this` がおかしい / 非同期の順番が想定と違う | **JS の問題** |
| `Type 'X' is not assignable to 'Y'` などコンパイルエラー | **TS の問題** |
| ビルドは通るのに画面が出ない | **環境/ツールの問題** |

---

## 3. 技術スタック

- **言語**: TypeScript(`strict: true` 必須)
- **ビルドツール**: Vite(`vanilla-ts` テンプレート)
- **描画**: Canvas 2D API(まずはこれで進める)
- **テスト**: Vitest(第2マイルストーン以降で導入)
- **実行環境**: 後述(WSL2 → Docker の順)

### セットアップコマンド

```bash
npm create vite@latest tetris-ts -- --template vanilla-ts
cd tetris-ts
npm install
npm run dev
```

---

## 4. 環境構築の方針(重要)

### 4.1 いきなり Docker にしない

最終的には Dev Container 化するが、**最初は WSL2 上に直接環境を立てる。**

理由: TypeScript とコンテナ内開発という2つの未知を同時に相手にすると、
エラーがどちらに由来するのか切り分けられなくなり、最も消耗するパターンになる。

**Docker 化は第3マイルストーンで行う。** それまでは WSL2 直で進める。

- プロジェクトは WSL2 のファイルシステム内(`~/projects/...`)に置く
- Windows 側(`/mnt/c/...`)には置かない — 極端に遅くなる

### 4.2 Docker 化するときの注意点

第3マイルストーンで Dev Container 化する際、以下は既知の落とし穴として先回りして対処すること。

1. **`node_modules` をバインドマウントに置かない** — Windows では著しく遅くなる。名前付きボリュームに分離する。
2. **ファイル監視が効かない場合がある** — バインドマウント越しでは inotify が届かず HMR が反応しない。`server.watch.usePolling` で回避。
3. **`vite --host` を付ける** — デフォルトは localhost バインドのため、ホストのブラウザから見えない。
4. **`forwardPorts` に 5173 を入れる**

参考の `devcontainer.json`:

```json
{
  "name": "tetris-ts",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:22",
  "forwardPorts": [5173],
  "postCreateCommand": "npm install"
}
```

---

## 5. コーディング規約

### 5.1 座標系(最優先で守ること)

**盤面は `board[y][x]` とする。** `y` が行(上から)、`x` が列(左から)。

衝突判定で最も時間が溶けるのは座標系の取り違えなので、
**盤面を扱う関数には必ずこの規約をコメントで明記する。**

### 5.2 型に関する方針

- `any` は原則禁止。使いたくなったら、まず理由を説明してもらう。
- **`number[][]` で済ませない。** 盤面やセルには意味のある型を付ける。
  - 例: セルは `type Cell = PieceKind | null` のようにする(`0`/`1` の数値で持たない)
- ゲーム状態は**判別可能ユニオン**で表現する。
  ```ts
  type GameState =
    | { kind: 'ready' }
    | { kind: 'playing'; board: Board; current: ActivePiece; score: number }
    | { kind: 'paused'; ... }
    | { kind: 'gameover'; score: number };
  ```
- `switch` による網羅チェック(exhaustiveness check)を意図的に活用する。
  `never` を使った網羅漏れ検出は必ず一度体験させること。
- 変更しない配列には `readonly` を付ける。

### 5.3 関数の粒度

ロジックは**純粋関数**として切り出す。描画や DOM 操作と混ぜない。

```ts
// 良い: テストできる
function canPlace(board: Board, piece: ActivePiece): boolean

// 悪い: 描画と判定が混ざっている
function moveAndDraw(): void
```

### 5.4 その他の細かいスタイル規約

- 空文字列は `""` に統一する(`" "` のような意味のない空白での代用はしない)。

---

## 6. スコープ管理

### 6.1 やらないこと(意図的な除外)

以下は **TypeScript の学習に寄与しない割に時間を大量に消費する** ため、初回スコープから外す。

- **SRS(Super Rotation System)のウォールキックテーブル**
  → 初期実装は「回転して置けなければ回転をキャンセルする」だけでよい
- ゴーストピース、ホールド機能、T-Spin 判定
- 対戦機能、サーバー通信
- 凝ったアニメーション、サウンド

これらは**全マイルストーン完了後の任意課題**とする。

ただし例外として、**ステップ8.5(非同期処理の学習)** ではスプライト画像の
`Promise` による非同期読み込みという最小限の実装のみを行う。
これは演出目的ではなく `async`/`await` を学習するための題材であり、
凝った演出やサウンド自体は引き続き完了後の任意課題のままとする。
学習者が途中でこれらを実装したがった場合、Claude は
「先にマイルストーンを終わらせることを勧める」と伝えること。

### 6.2 逆に必ずやること

**localStorage への スコア保存と読み込み時の検証。**

これは機能としては地味だが、このプロジェクトで唯一
「外部から来る `unknown` なデータを絞り込んで型を付ける」経験ができる箇所であり、
実務の TypeScript で最も出番が多い領域なので**省略してはならない**。

あわせて、型ガードを引数に取る**ジェネリック関数**(`loadFromStorage<T>` など)を
自力で定義する経験もここで行う。`ReadonlyArray<T>` のように既製のジェネリクスを
「使う」経験はステップ4までで済んでいるため、ステップ10では「自分で定義する」側を扱う。

---

## 7. マイルストーンとステップ

想定合計: **38〜68時間**(1日1〜2時間ペースで 1.5〜2.5ヶ月)

### 第1マイルストーン: 「ブロックが落ちて積み上がる」(15〜20h)

ここに到達すれば「作れている」実感が出る。最も重要な区切り。

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 1 | WSL2 + Vite 環境構築 | 1〜3h | `npm run dev` でブラウザに何か表示される |
| 2 | TS 素振り(tsconfig / strict / ユニオン型) | 5〜10h | 判別可能ユニオンを自力で書ける |
| 3 | Canvas 描画とゲームループの土台 | 4〜8h | 四角形が一定間隔で下に動く |
| 4 | コアの型設計(Piece / Board / GameState) | 2〜4h | `any` なしで盤面とピースを表現できている |
| 5 | 落下と衝突判定 | 4〜8h | ブロックが底と既存ブロックで止まり、積み上がる |

**ステップ5 は最難関。** 座標系の規約(5.1)を必ず先に確認すること。

### 第2マイルストーン: 「遊べる」(12〜19h)

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 6 | 回転(簡易版・キック無し) | 3〜6h | 回転できる。壁際で置けなければ何も起きない |
| 7 | ライン消去とスコア | 2〜4h | 揃った行が消え、上が落ちてくる |
| 8 | キー入力と落下速度調整 | 3〜5h | 左右移動・ソフトドロップ・ハードドロップが効く |
| 8.5 | 非同期処理(スプライト画像の読み込み) | 2〜4h | ピース画像を `async`/`await` で読み込んでから盤面が表示される。読み込み失敗時に分かりやすいエラーが出る |

このマイルストーンから **Vitest を導入**し、
ステップ5〜7で書いた純粋関数にテストを書く。テストは学習者に書かせること。

### 第3マイルストーン: 「仕上げる」(9〜19h)

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 9 | 状態遷移(ゲームオーバー / リスタート) | 2〜4h | `GameState` の網羅チェックが効いている |
| 10 | localStorage + `unknown` の検証 + ジェネリクス | 3〜5h | 壊れた JSON を入れてもクラッシュしない。型ガードを受け取る汎用関数 `loadFromStorage<T>` を自力で書ける |
| 11 | Dev Container 化 | 3〜8h | コンテナ内で HMR が動き、補完も効く |
| 12 | GitHub Actions で CI | 1〜2h | `tsc --noEmit` と `vitest run` が回る |

---

## 8. CI に関する注意

**Vite は型チェックをしない。** esbuild は型を無視して剥がすだけなので、
型エラーがあっても dev サーバーは平気で動き、`npm run build` も通ってしまう。

したがって CI には必ず以下を独立したステップとして入れる。

```yaml
- run: npx tsc --noEmit
- run: npx vitest run
```

`package.json` にもスクリプトとして登録しておく。

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
```

---

## 9. 進捗の目安について

- **第1マイルストーンに2週間かかっても正常。** 遅いと判断しないこと。
- 環境構築でつまずいて離脱するのが最も多い失敗パターンなので、
  ステップ1で3時間以上溶けている場合、Claude は
  **Docker やツールの追求をやめて先に進むこと**を提案する。
- 各ステップ完了時に、学習者に「この回で新しく理解した型の概念」を一言で言わせる。
  言えなければ、そのステップはまだ終わっていない。

---

## 10. 第4マイルストーン: 開発ワークフローの強化(全12ステップ完了後の追加課題)

**成果物ではなく学習が目的、という1節の方針はここでも変わらない。**
第1〜第3マイルストーン(ステップ1〜12)完了後に追加する。6.1節の「任意課題」(SRSウォールキック、ゴーストピースなどのゲーム機能)とは別に、開発ワークフロー・公開・型安全性をさらに強化する学習として位置づける。

### フェーズ1: ESLint + Prettier 導入

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 13 | ESLint + Prettier 導入 | 3〜5h | `lint`/`lint:fix`/`format` スクリプトが動き、CI に独立した lint ステップがある。既存コードの lint エラーがルールごとに判断され解消されている |

**背景**: tsconfig は `strict: true`、CI で `tsc --noEmit` と `vitest run` が回っている状態。Lint が未導入なので追加する。

**やること**:
1. `eslint` / `typescript-eslint` / `prettier` / `eslint-config-prettier` を devDependencies に追加する
2. Flat Config(`eslint.config.js`)で設定する。型情報を使うルール(typescript-eslint の `recommendedTypeChecked`)を有効化する
3. Prettier と競合するルールは `eslint-config-prettier` で無効化する
4. `package.json` に `lint` / `lint:fix` / `format` スクリプトを追加する
5. `.github/workflows/ci.yml` に lint ステップを、`typecheck` / `test` と独立したステップとして追加する

**進め方の注意(重要)**:
- 設定ファイル自体は2.2節の「設定ファイル」扱いとして Claude が書いてよい。
- **既存コードに lint エラーが出た場合、自動修正で一括で潰さない。** まず「どのファイルでどのルールが何件出たか」を一覧で見せ、ルールごとに「なぜそのルールがあるのか」を説明してから、1ルールずつ「自動修正でよいか / 手で直すべきか」を学習者に判断させる。
- 特に typescript-eslint の型情報ベースのルール(`no-floating-promises`, `no-misused-promises` など)で引っかかった場合は、非同期処理の理解を深めるチャンスとして丁寧に扱う。

### フェーズ2: PRフロー + 公開 + デプロイ

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 14 | PRベースの開発フローを一度体験する | 1〜2h | 作業ブランチ → PR作成 → CI緑 → マージの流れを実際に経験し、branch protection の設定方法を理解している |
| 15 | リポジトリを公開する | 1〜2h | 公開前のセキュリティチェック(秘匿情報の有無)が完了し、README がポートフォリオとして機能する内容になっている |
| 16 | GitHub Pages にデプロイする | 1〜3h | main へのマージ時に build → Pages への自動デプロイが動き、公開URLでプレイできる(スプライト画像のサブパス配信を含む) |

**目的**: これまで main に直 push していたので、CI がゲートとして機能する形を実際に体験し、最終的に「人に見せられる状態」にする。

**ステップ14(PRフロー)**:
- 適当な作業ブランチを切る(フェーズ1の lint 導入をこのブランチで行ってもよい)
- PR を作成し、CI が走って緑になることを確認してからマージする
- GitHub の branch protection(「CI が通らないと main にマージできない」)は、学習者が GitHub の画面から自分で設定する。`gh` CLI は未導入のため、Claude は手順を説明するのみで自動操作はしない

**ステップ15(公開)**:
- public にする前に、コミット履歴に秘匿情報(パス、トークン、個人情報)が含まれていないかを Claude がチェックする
- README に含める内容: 何を作ったか / デモURL / スクリーンショット、技術選定(TypeScript strict, Vite, Vitest, Dev Container, GitHub Actions)、設計上の判断(判別可能ユニオンでの状態管理、純粋関数とレンダリングの分離、localStorage の unknown 検証など)、学習用プロジェクトであることと work-log.md へのリンク
- **README 本文は学習者が自分の言葉で書く。** Claude は構成案と「ここに何を書くか」のガイドを出すに留め、本文は書かない。ただし技術選定の記述に事実誤認があれば指摘する

**ステップ16(デプロイ)**:
- `vite.config.js` の `base` をリポジトリ名に合わせる
- `.github/workflows` に deploy 用ワークフローを追加する(main へのマージ時に build → Pages へデプロイ)
- スプライト画像(`public/sprites/*.svg`)がサブパス配信でも正しく読み込まれるか確認する

**進め方の注意**: ステップ14 → 15 → 16 の順で、各段階で一度止まって学習者に確認を取ってから次に進む。

### フェーズ3: `noUncheckedIndexedAccess`

| # | ステップ | 目安 | 完了条件 |
|---|---|---|---|
| 17 | `noUncheckedIndexedAccess` の導入 | 2〜4h | 追加後も `tsc --noEmit` が通り、既存テスト全ケースが通り続けている。各修正箇所でアサーション/フォールバック/ガード/データ構造変更のどれが適切かを判断できている。ただし「このプロジェクトには重すぎる」という判断で見送ることも許容する |

**目的**: このプロジェクトでは「配列の範囲外アクセスで実行時 TypeError」「添字アクセスの型が `number` のまま」という問題を何度か踏んでいる(ステップ5, 7 の work-log 参照)。それを型で守れるフラグを実際に体験する。

**進め方**:
1. まず `noUncheckedIndexedAccess` が何をするフラグで、なぜ `strict` に含まれていないのかを Claude が説明する
2. `tsconfig.json` に追加して `tsc --noEmit` を実行し、出たエラーを「ファイルごと・パターンごと」に分類して見せる
3. 分類したパターンごとに、**修正は学習者にやらせる(`TODO(human)` 方式)。** ただし最初の1パターンだけは Claude が手本を見せる
4. 特に見るべき箇所:
   - `collision.ts` の `canPlace` / `lockPiece`(`board[pos.y][pos.x]` を触っている箇所)
   - `lines.ts` の `calculateScore`(ルックアップテーブルの添字アクセス。「1ピース=4マスだから最大4」という不変条件があるため、型と実行時の不変条件がズレる典型例)
   - `rotation.ts` の `PIECE_SHAPES` / `PIECE_BOX_SIZE` へのアクセス
5. 各箇所で「非 null アサーション(`!`)で握りつぶす」「`??` でフォールバック」「早期リターンでガードする」「そもそもデータ構造を変える」のうちどれが適切かを、ケースごとに学習者に判断させる。安易な `!` の使用には Claude が指摘する
6. 既存テスト(全42ケース)が通り続けることを確認しながら進める
7. エラーが多すぎて学習にならないと判断した場合、「このフラグは今回のプロジェクトには重すぎる」という結論で終えることも許容する

各フェーズ完了後は `work-log.md` に追記する。
